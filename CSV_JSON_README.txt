CLAIMTRACE — CSV + JSON DATA PACKAGE

All original CSV files are preserved unchanged.
For every CSV file, a matching JSON file has been generated using the CSV header as JSON keys and each row as one JSON object.

The JSON files are intended for web/Google AI Studio development, while CSV files remain useful for analysis and data processing.
